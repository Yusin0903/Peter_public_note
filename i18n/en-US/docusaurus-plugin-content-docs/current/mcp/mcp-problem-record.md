---
sidebar_position: 1
---

# MCP Usage Notes

## GitHub MCP Requires Docker

When using the GitHub MCP server, make sure Docker is running first.
If you're using OrbStack (a Docker Desktop alternative on macOS), start OrbStack before using the MCP server.

```bash
# Check if Docker is running
docker ps

# If no response, start OrbStack or Docker Desktop first
```
